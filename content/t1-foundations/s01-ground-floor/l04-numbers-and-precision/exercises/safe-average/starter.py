def all_within(values, target, tolerance):
    """True when every value is within `tolerance` of `target`."""
    # TODO: compare each value's distance from target against tolerance.
    # Do not use == and do not import math.isclose.
    return False
