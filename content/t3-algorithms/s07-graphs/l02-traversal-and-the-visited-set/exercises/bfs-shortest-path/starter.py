from collections import deque


def shortest_path(graph, start, goal):
    """A path with the fewest edges from start to goal, or None."""
    # TODO: BFS out from `start`, recording for each newly discovered node the
    # node you reached it from. When you discover `goal`, walk those recorded
    # links back to `start` and reverse.
    return None
