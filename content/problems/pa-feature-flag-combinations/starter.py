def feature_sets(features):
    """Every combination of the optional features, including the empty one."""
    # TODO: split on the first feature. Every combination either leaves it out
    # or includes it, and both halves come from the same function on the rest.
    return [[]]
