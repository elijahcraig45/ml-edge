def runs_summing_to(deltas, target):
    """Count contiguous stretches whose values total exactly target."""
    found = 0
    # TODO: keep a running total and a dict counting how often each running
    # total has already occurred. Seed the dict with the empty prefix.
    return found
