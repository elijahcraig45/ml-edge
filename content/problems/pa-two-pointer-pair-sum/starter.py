def pair_summing_to(counts, target):
    """Indices of two entries summing to target, or None."""
    # TODO: the list is sorted. Start one index at each end and decide, from the
    # sum you see, which end can be moved without losing a possible answer.
    return None
