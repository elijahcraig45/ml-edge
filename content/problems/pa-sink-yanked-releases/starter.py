def sink_yanked(names):
    """Move every None to the end in place, keeping the rest in order."""
    # TODO: read every slot with one index; keep a second index pointing at the
    # first slot a survivor has not yet claimed.
    return None
