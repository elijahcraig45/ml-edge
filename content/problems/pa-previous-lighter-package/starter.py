def previous_lighter(sizes):
    """Index of the closest earlier strictly smaller package, else -1."""
    answer = []
    # TODO: keep a stack of indices whose sizes increase from bottom to top.
    # Anything at least as big as the current package can be discarded for good.
    return answer
