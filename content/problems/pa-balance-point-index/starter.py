def balance_day(daily):
    """Earliest index whose left and right sums are equal, else -1."""
    # TODO: compute the grand total once, then track only the running sum to the
    # left. The right-hand sum is what remains after removing the current day.
    return -1
