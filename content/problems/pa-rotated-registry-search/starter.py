def find_rotated(values, target):
    """Index of target in a rotated sorted list of distinct ids, else -1."""
    # TODO: at each midpoint, one half is still sorted. Work out which, then use
    # its endpoints to decide whether the target can be inside it.
    return -1
