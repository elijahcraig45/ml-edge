def pair_within_budget(sizes, budget):
    """Indices of two packages whose sizes add up to budget, or None."""
    # TODO: for each package, work out which size would complete it, and check
    # whether you have already walked past a package of that size.
    return None
