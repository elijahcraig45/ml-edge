def tree_depth(node):
    """Depth of a dependency tree, counting the root as level 1."""
    if node is None:
        return 0
    # TODO: express a package's depth in terms of its dependencies' depths.
    return 1
