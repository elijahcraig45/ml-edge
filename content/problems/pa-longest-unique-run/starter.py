def longest_unique_run(events):
    """Length of the longest stretch with no repeated value."""
    best = 0
    # TODO: advance a right edge one event at a time and move the left edge only
    # as far as the newest duplicate forces it.
    return best
