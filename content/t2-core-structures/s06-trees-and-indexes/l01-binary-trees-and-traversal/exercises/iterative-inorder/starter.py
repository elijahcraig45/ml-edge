def inorder_iterative(root):
    """Inorder values, using an explicit stack rather than recursion."""
    out = []
    stack = []
    node = root
    # TODO: descend left pushing as you go, then pop, emit, and go right.
    return out
