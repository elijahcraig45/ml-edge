def find_pair(sizes, target):
    """Indices of two entries summing to target, or None. `sizes` is sorted."""
    lo, hi = 0, len(sizes) - 1
    # TODO: walk the two pointers inward. Which one moves depends on whether the
    # current sum is too small or too large.
    return None
